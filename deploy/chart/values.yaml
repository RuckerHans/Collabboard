migrations:
  run: false

redis:
  image: redis:7-alpine
  replicas: 1

postgres:
  image: postgres:16-alpine
  storage: 1Gi
  credentials:
    database: collabboard
    user: collabboard
    password: collabboard123
    appPassword: collabboard_app_pw123

api:
  image:
    repository: collabboard-api
    tag: kind
  replicas: 3
  jwtSecret: local-dev-collabboard-secret
  config:
    corsOrigin: http://localhost
    jwtExpiry: 7d
    googleCallbackUrl: http://localhost/api/auth/google/callback

front:
  image:
    repository: collabboard-front
    tag: kind
  replicas: 1